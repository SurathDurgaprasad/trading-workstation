# Final Release Remaining Work

Prioritized against the release-gate mission's own P0-P3 order. Each
item below is grounded in file:line evidence gathered by a fresh
read-only codebase survey against the CURRENT code (not the prior
reports), run at the start of this phase. Status is updated in place
as items are closed; nothing is marked closed without a fix + a real
test proving it (KNOWN DEFECT -> ROOT CAUSE -> CODE FIX -> REGRESSION
TEST -> FAILURE-INJECTION TEST WHERE APPROPRIATE -> FULL REGRESSION ->
DOCUMENT -> CLOSE, per the mission's hard release rule).

---

## P0 -- data correctness, time correctness, trading safety, state/persistence, live-trading prevention

| # | Item | Evidence | Status |
|---|---|---|---|
| P0-1 | **No OHLCV data-quality validation layer exists anywhere.** No check for high>=low/open/close, low<=open/close, duplicate timestamps, timestamp monotonicity, or gap/missing-bar detection. `OHLCV.from_dataframe` only drops NaN rows. | `market/data_provider.py:104-141` (no sort/dedup/monotonic check, repo-wide grep confirms no such logic exists elsewhere) | **CLOSED** -- root fix: `OHLCV.from_dataframe` now also drops rows with an impossible OHLC relationship (`market/data_provider.py::_row_has_valid_ohlc_relationship`), the same mechanism it already used for NaN rows. New `market_data/validation.py::validate_ohlcv` adds series-level HEALTHY/DEGRADED/INVALID checks (duplicates, ordering, gaps, symbol identity, staleness). 34 new tests (`tests/test_market_data.py`, `tests/test_market_data_validation.py`). |
| P0-2 | **INVALID data is not structurally prevented from reaching prediction/decision/paper-trade.** | Same as P0-1 | **CLOSED** -- `market_intelligence/scanner.py`'s `_screen_symbol`/`_fetch_benchmark` (the actual scan -> `decision_engine` -> paper-trade choke point) now call `validate_ohlcv` right after fetch and exclude the symbol (`ExcludedCandidate`, never scored) on INVALID, the same pattern already used for a fetch failure. New failure-injection test: `test_invalid_data_quality_is_excluded_not_scored`. |
| P0-3 | **Two missed datetime-normalization duplication spots**, reimplementing `core.timeutil.as_utc_aware`'s exact logic inline instead of importing it -- the same bug class the consolidation was meant to close. | `dashboard/app.py:220-222`, `live/dhan/clock_skew.py:94-96` | **CLOSED** -- both now delegate to `core.timeutil.as_utc_aware`; targeted tests re-confirmed passing (zero behavior change). |
| P0-4 | **Paper position state transitions are not guarded.** `paper/store.py::update_position()` has no `WHERE status = 'OPEN'` guard and no CHECK constraint; `paper/engine.py::_close_position()` never re-verifies the position is still OPEN before closing. A closed->open or double-close transition is prevented only by caller discipline, not by the data layer. | `paper/store.py:232-236`, `paper/engine.py:331-384`, `paper/models.py:76-78` | **CLOSED** -- `update_position()` now guards with `WHERE status != 'CLOSED'` and raises a new `paper.errors.InvalidPositionTransitionError` (loud, not a silent no-op) on a 0-row update against an existing position; `ValueError` for a nonexistent one. 4 new tests in `tests/test_paper_store.py`; full `paper/` suite (135 tests) re-confirmed passing with no behavior change on the normal path. |
| P0-5 | **Live order execution: re-confirmed structurally blocked.** Zero `requests.post/put/delete` anywhere in the repo; `DisabledDhanOrderExecutor` unconditionally raises on every order method; it is constructed only in `tests/test_dhan_no_real_orders.py`, never in production code; no MCP tool places orders. | `live/dhan/broker_adapter.py:28-37,66-73`, repo-wide grep, `tests/test_dhan_no_real_orders.py` | **CLOSED -- verified this phase, no code change needed** |
| P0-6 | **LLM/Ollama failure isolation: re-confirmed correct.** `decision_engine/engine.py` and `research/summarizer.py` both wrap LLM calls in try/except that degrades to a non-fatal `narrative_unavailable_reason` field rather than raising. | `decision_engine/engine.py:109-117`, `research/summarizer.py:110-117` | **CLOSED -- verified this phase, no code change needed** |

## P1 -- failure recovery, database integrity, provider resilience, paper execution, scheduler

| # | Item | Evidence | Status |
|---|---|---|---|
| P1-1 | **Predictions duplicate prevention is application-level only, non-atomic.** `predictions/store.py::has_prediction_for_entry()` and `direction_forecast_store.py::has_forecast_for_bar()` are check-then-insert with no spanning transaction and no DB `UNIQUE` constraint on the real natural key (symbol+entry_time / symbol+as_of) -- only a PK on a UUID surrogate. Two concurrent callers (manual run overlapping a scheduled run) could both pass the check and double-insert. | `predictions/store.py:24-30,86-98`, `predictions/direction_forecast_store.py:21-26,82-87` | **CLOSED** -- both stores now migrate in a real `entry_time`/`as_of` column (via new shared `core.sqlite_util.ensure_column`, backfilled from each pre-existing row's own `data_json`, never guessed) and attempt a genuine `UNIQUE(symbol, entry_time\|as_of)` index (new shared `core.sqlite_util.try_create_unique_index`) -- soft-fails to `duplicate_prevention_enforced_at_db_level = False` rather than crashing startup or deleting data if an already-deployed DB has pre-existing duplicates. `save_prediction`/`save_forecast` now raise typed `DuplicatePredictionError`/`DuplicateForecastError` on a real constraint hit. Confirmed both call sites in `main.py` already check `has_prediction_for_entry`/`has_forecast_for_bar` first, so this is purely a race backstop, not a behavior change on the normal path. 8 new tests across `tests/test_predictions_store.py`/`tests/test_direction_forecast_store.py`/`tests/test_core_sqlite_util.py`, including a simulated pre-migration on-disk schema and a simulated pre-existing-duplicate database. `live/state_store.py`'s own pre-existing `_ensure_column` was consolidated into the same shared helper it now shares with these two stores. |
| P1-2 | **No schema-version tracking on any of the 12 stores** (`PRAGMA user_version` or equivalent -- zero repo-wide hits). Only `live/state_store.py` has a migration helper (`_ensure_column`); the other 11 have none. | Table gathered this phase (12 stores audited) | **CLOSED**. The additive-column migration helper (`live/state_store.py`'s former private `_ensure_column`) is now a shared `core.sqlite_util.ensure_column`, used by the 3 stores that have ever needed a real column addition (`live/state_store.py`, `predictions/store.py`, `predictions/direction_forecast_store.py` -- see P1-1). `integrity_check()`/`db_size_bytes()` are now on **all 12** stores. `core.sqlite_util.get_schema_version`/`set_schema_version`/`ensure_schema_version` (`PRAGMA user_version` -- zero-cost, no bootstrap table needed) were added and wired into **all 12 stores'** `__init__` methods, each stamping its own `CURRENT_SCHEMA_VERSION` and exposing `schema_version()`; a fresh-DB test and an old-preexisting-database-gets-upgraded-on-open test exist for the primitive itself plus a dedicated test per store. Migration itself remains additive-column-only (no destructive/renaming migration exists, by design -- nothing in this project has ever needed one, and inventing one speculatively would be exactly the kind of unjustified complexity this campaign's own "finish, don't expand" rule argues against); the mechanism is a single, shared, tested primitive available to every store the moment any of them needs a real schema change, which is what "prefer one small shared mechanism" asked for. 37 new tests total across `tests/test_core_sqlite_util.py` and all 12 stores' own test files. |
| P1-3 | **Unified health system does not exist.** `readiness-check` covers only some subsystems (Dhan credential presence, one DB, kill switch, session/holiday, disk-write probe); `cache-status` is separate; Ollama health (`check_ollama_availability`) is never called from `readiness-check`; store integrity is not surfaced from `readiness-check` at all (though `integrity_check()` itself is now on all 12 stores, per P1-2). | `main.py:2970-3079` (`readiness-check`), `main.py:2908` (`cache-status`), `llm/provider.py:98` | **CLOSED** -- new `core/health.py::collect_system_health()`, a pure function returning a per-component + overall HEALTHY/DEGRADED/SAFE_STOP/FAILED `SystemHealth`, consumed identically by a new `python main.py health` CLI command AND a new dashboard `/health` route (the mission's own explicit "do not create separate incompatible implementations" requirement). Covers: application, database (`PRAGMA integrity_check` across every store whose db file exists), disk, dhan (credential presence), kill_switch, scheduler (lock state), risk (config loads), and ollama (reachability + required models -- a real, previously-disclosed gap, now closed). Deliberately does NOT make a live Yahoo/Dhan network call by default (`readiness-check --deep` remains the place for that) and does not implement memory-pressure monitoring (no new dependency added for it -- disclosed, not silently omitted). `readiness-check` itself is unmodified -- this is new, additive coverage, not a risky rewrite of an already-tested command. 27 new tests across `tests/test_core_health.py`, `tests/test_cli_health.py`, `tests/test_dashboard_health.py`. |
| P1-4a | `last_success_at`/`last_failure` tracking per scheduler job. | Carried from `FINAL_FAILURE_MODE_ANALYSIS.md` #17 | **CLOSED**. `SchedulerRunStore` gained `last_successful_run_for_slot()`/`last_failed_run_for_slot()`/`distinct_slot_names()`, surfaced in `schedule status`'s new "Per-slot summary" section (last success timestamp, last failure timestamp + its error, per configured slot) -- 8 new tests across `tests/test_scheduler_store.py`/`tests/test_cli_schedule.py`. |
| P1-4b | Per-job scheduler timeout. | Carried from `FINAL_FAILURE_MODE_ANALYSIS.md` #17 | **ACCEPTED LIMITATION, documented**. `scheduler/runner.py::_execute_slot` calls `main.py` command functions in-process and synchronously (no subprocess, no thread) -- a naive `threading.Thread(...).join(timeout=...)` wrapper would not actually stop a slow/hung call (e.g. a blocked network request inside it); the tick would report "timed out" while the original call keeps running unsupervised in the background, potentially completing its own DB writes after the scheduler has already moved on and started something else -- a worse failure mode than the current one (a hang that is at least visible as a stuck RUNNING lock, reclaimed by `reclaim_stale_locks` on the next tick, itself verified across a real process restart -- see P1-5). A REAL preemptive timeout needs subprocess isolation, a genuinely larger architectural change than this item's own scope justifies without separate evidence the in-process design cannot be hardened another way. Documented in `OPERATIONS_GUIDE.md`'s own scheduler section so an operator knows the actual recovery path (restart the process) rather than expecting a timeout that doesn't exist. |
| P1-4c | Overlapping-slot fairness. | Carried from `FINAL_FAILURE_MODE_ANALYSIS.md` #17 | **CLOSED -- was already correct, now documented**. `ScheduleConfig.due_slot()` was already deterministic (first-due-in-configured-order, verified by pre-existing `tests/test_scheduler_config.py::test_due_slot_returns_first_due_in_configured_order`) -- the "gap" was that this behavior was documented only in the function's own docstring, not anywhere an operator configuring a custom YAML schedule would see it. Added to `OPERATIONS_GUIDE.md`. |
| P1-5 | Restart-recovery coverage across every stateful category (predictions, forecasts, positions, capital, kill switch, scheduler locks, experiment/model metadata) and duplicate-on-replay-after-restart. | New this phase -- a dedicated coverage survey | **CLOSED**. Survey found EXISTS for 6 of 8 categories already (predictions, direction forecasts, paper positions+capital, kill switch, experiment/promotion/registry metadata -- all via genuine two-instance-same-file restart tests). Two real gaps found and closed: (1) `reclaim_stale_locks` had only ever been exercised on the SAME `SchedulerRunStore` instance that started the run, never across a real crashed-process-then-restarted-process boundary -- new `test_reclaim_stale_locks_works_across_a_real_process_restart`. (2) No test verified that replaying an already-processed bar/signal AFTER a restart doesn't duplicate a position/order/fill (existing duplicate-bar tests only ever used one continuous in-process engine instance) -- new `test_replaying_an_already_processed_bar_after_a_restart_does_not_duplicate_state` in `tests/test_paper_restart.py`, proving the persisted `bar_cursor`, not in-memory state, is what makes this safe. |
| P1-6 | Provider failure-injection matrix (market data/Yahoo, Dhan, Ollama). | Mission's own explicit ask | See dedicated coverage survey below this table -- status recorded once that survey's findings are triaged. |
| P1-7 | Data-failure-must-never-become-a-trade, proven end-to-end through real production wiring. | Mission's own explicit ask | **CLOSED**. New `test_shadow_run_invalid_market_data_never_reaches_decision_prediction_or_paper_order` in `tests/test_shadow_run.py` drives the REAL `run_shadow_run_command` with `--paper-execute` across two symbols (one with a duplicate-timestamp INVALID series, one healthy) -- confirms zero decisions/predictions/paper orders exist for the INVALID symbol while the healthy one proceeds through the full scan -> decision -> risk -> paper-execution pipeline normally. Only the market-data provider is faked; every other stage (scanner, decision engine, risk sizing, critic, paper engine) is real production code, not a reimplementation. |
| P1-8 | LLM/Ollama failure must never block the deterministic core ("LLM = DEGRADED, CORE = OPERATIONAL"). | Mission's own explicit ask | **CLOSED -- already correct, re-verified this phase, no new test needed**. Both real LLM call sites (`decision_engine/engine.py::make_decision`, `research/summarizer.py::build_research_report`) already have dedicated, deterministic tests (`test_make_decision_degrades_gracefully_when_ollama_unavailable`, `test_build_research_report_degrades_gracefully_when_ollama_unavailable`) that monkeypatch `check_ollama_availability` to raise and confirm the decision/report still completes with a `narrative_unavailable_reason`/no-AI-summary marker rather than raising. A third, full-pipeline (`shadow-run --with-ai` with Ollama down) end-to-end test was considered and deliberately not built -- the exact mechanism being proven (a try/except boundary around one function call) is already covered at the two real call sites with dedicated tests; a third end-to-end version would re-prove the same architectural guarantee already proven twice, for real added test-maintenance cost and no new coverage. |
| P1-9 | Paper-trading cross-store state consistency ("the same event must not produce contradictory state in different stores"). | Mission's own explicit ask | **CLOSED -- already existed, confirmed this phase, not rebuilt**. `paper/reconciliation.py::reconcile()` is a real, deterministic, already-existing cross-check (equity = cash + position value, independently re-derived from raw fields rather than trusting a computed property; realized P&L matches the trade ledger sum; no negative quantities; peak equity is never below current equity) -- confirmed wired into production code at 6 real call sites (`main.py` x4, `live/workstation.py`, `mcp_server/server.py`), not just tests. This module's own docstring already states its philosophy exactly matches the mission's own ask: "FAIL LOUDLY. Do NOT silently repair financial state." No new work needed; the P1-7 end-to-end test above additionally exercises this same infrastructure. |

## P2 -- unified health, observability, installation, operations, security

| # | Item | Evidence | Status |
|---|---|---|---|
| P2-1 | Installation docs (`README.md`, `requirements.txt`, `.env.example`) re-verified sufficient for a clean install from scratch; `.env.example` covers the one real env-var surface (Dhan credentials) completely -- LLM/Ollama config is hardcoded, not env-driven, so nothing is missing there either. | `README.md:75-132`, `.env.example`, `core/config.py` | **CLOSED -- verified this phase, no gap found** |
| P2-2 | Dedicated `INSTALLATION.md`/`USER_GUIDE.md`/`OPERATIONS_GUIDE.md`/`TROUBLESHOOTING.md`/`ARCHITECTURE.md`/`SECURITY.md` document suite. | Carried from `FINAL_PRODUCT_READINESS_REPORT.md` | **CLOSED** -- all 6 written, describing the system as it currently exists (cross-checked against real code, not aspirational), cross-linked from `README.md`. |
| P2-3 | Security re-audit (secret scan, dependency audit, log redaction audit). | Carried from prior phase | **CLOSED** -- `pip-audit` run against `requirements.txt`: 7 CVEs found across `langchain`/`chromadb`, both investigated (not reflexively bumped or ignored) -- `langchain` bumped 1.3.4->1.3.9 (its CVE not exploitable here, bumped anyway as defense-in-depth); `chromadb` left pinned (its 4 CVEs are all in a networked server mode this project never starts, no fix version exists). Full findings and reasoning in `SECURITY.md`. Repo-wide secret scan re-run, clean. Log-redaction already covered by existing `_redact_secret`/masked-`__repr__` mechanisms, re-confirmed via `tests/test_dhan_credential_security.py`. |
| P2-6 | Configuration validation audit -- every config value should have type/validation/a safe default/a description; invalid config should fail clearly, never silently fall back to a dangerous default; credentials never logged. | New this phase -- a dedicated configuration audit | **CLOSED**, mixed real fixes and one formally-accepted, disclosed limitation. `risk/config.py`, `critic/config.py`, `decision_engine/config.py`, `market_intelligence/config.py` were already frozen pydantic models with `Field(gt=/ge=/le=, description=...)` on every field -- confirmed, no gap. `scheduler/config.py::ScheduleSlot` (a plain dataclass) had a real gap -- malformed *types* raised clearly, malformed *logic* (an inverted `[after, before)` window, a non-positive `frequency_minutes`) did not -- fixed via `__post_init__` validation raising `SchedulerConfigurationError`, 8 new tests (see P0-3-adjacent work). 9 previously-undocumented `TRADING_*_DB_PATH` override env vars were found read but absent from `.env.example` -- added. `live/dhan/config.py`'s credential handling was independently re-confirmed already correctly hardened (masked `__repr__`, redacted logs, no hardcoded literals). **`core/config.py`'s `Settings` (the LLM/RAG advisory-layer config) has no per-field range validation -- formally ACCEPTED LIMITATION, not fixed**: `Settings` is constructed exactly once, with zero arguments (`get_settings() -> Settings()`), confirmed via repo-wide grep to be the ONLY construction site -- there is no untrusted-input path to defend against today, and adding range constraints to a config class nothing ever feeds dynamic input into would be exactly the kind of unjustified complexity this campaign's own "finish, don't expand" rule argues against. Documented in `SECURITY.md` with the explicit condition under which this should be revisited (if `Settings` is ever constructed from CLI args, env vars, or any other external input). |
| P2-4 | Clean installation from a genuinely fresh environment (the mission's own "mandatory release gate"). | New this phase | **CLOSED** -- a real `git clone` of the pushed branch into an isolated temp directory, a brand-new venv, `pip install -r requirements.txt`, and a single `pytest` run: 2020 passed, 0 failed, 82 skipped cleanly (matching README's own claim). Found and fixed a REAL defect this way (not merely confirmed): `langchain==1.3.9` requires `langchain-core>=1.4.6`, but `requirements.txt` still pinned `langchain-core==1.4.1` -- an unsatisfiable combination invisible to the existing dev venv (which had already been silently upgraded by the earlier `pip install langchain==1.3.9` command) but fatal to a genuinely fresh install. Fixed by pinning `langchain-core==1.6.3` (the actual, tested-compatible version). Also found and fixed a real, minor accuracy bug in `core/health.py`'s database-check message (reported the count of *configured* store paths, not the count actually found and checked) via a `python main.py health` smoke test against the same clean install. Further smoke-tested `universe`, `scan` (real Yahoo data), and `paper status` successfully in that same environment. |
| P2-5 | Claude Code / AI-agent runtime independence. | New this phase | **CLOSED** -- a dedicated read-only audit found zero hidden runtime dependency (no `claude`/`anthropic` imports outside one test's own independence-assertion docstring, no agent-specific env vars read anywhere, `mcp_server/` confirmed opt-in and never imported by `main.py`'s core command set). Then independently re-verified by direct execution: `.claude/`, `.cursor/`, `.cursorignore`, and `.mcp.json` were all deleted from the clean-install clone (P2-4) and `health`, `universe`, `scan`, and `paper status` all ran identically with zero trace of any AI-tool artifact present -- proof by removal and re-execution, not just static analysis. |

## P3 -- performance, UX, research/intelligence

Explicitly not touched this phase, per the mission's own rule ("do not
work on P3 while a P0/P1 release blocker remains"). The Phase 1
research verdict (no demonstrated economic edge) remains frozen and is
not being revisited.

---

## Defects found via the regression discipline itself (not pre-planned)

- **Fixture bugs exposed by the new P1-1 DB constraint** (not a defect in the constraint): `tests/test_predictions_store.py` (4 tests) and `tests/test_cli_experiment.py` (2 tests, via its shared `_seed_resolved_predictions` helper) used a hardcoded, identical `entry_time` across multiple predictions for the same symbol -- data no real caller would ever legitimately produce (has_prediction_for_entry would have refused it), but previously allowed because no DB-level constraint existed to catch it. Root-caused (not weakened/skipped): each fixture now varies `entry_time` per prediction; `_seed_resolved_predictions` additionally gained an `entry_day_offset` parameter so its two same-symbol call sites (baseline vs. candidate config) cannot collide with each other either. Full regression re-confirmed 0 failures after the fix. This is exactly the kind of real, load-bearing correctness gap the new constraint was built to surface -- the fixtures were quietly relying on behavior the DB-level guarantee now correctly forbids.

## Execution plan for this phase

Given real session scope, this phase focuses on closing every P0 item
that is actually open (P0-1 through P0-4) with real fixes and tests,
plus the highest-value, well-scoped P1 item (P1-1, since it's a small,
targeted DB constraint addition directly adjacent to work already done
this session on the same stores). P1-2/P1-3/P1-4 and all of P2 beyond
verification are being explicitly deferred -- each is a genuinely large
undertaking (a shared migration framework across 12 stores; a unified
health service spanning 10+ subsystems) that would not receive the
same fix-with-tests rigor as the P0 items if compressed into this same
session. This is disclosed here, not silently dropped, and will be
carried into the final release-candidate report as a known remaining
blocker to a "READY" verdict.
