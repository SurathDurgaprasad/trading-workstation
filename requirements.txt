# Direct dependencies actually imported by this project's code, pinned to
# the versions already installed in venv/ at the time of the Phase 1/2
# audit-and-extend pass. This file did not exist before; it documents the
# existing environment rather than introducing new pins.
#
# venv/ has many more (transitive) packages than this — this file is the
# direct-dependency contract, not a full `pip freeze`.

langgraph==1.2.4
langchain==1.3.4
langchain-core==1.4.1
langchain-community==0.4.2
langchain-chroma==1.1.0
langchain-ollama==1.1.0
langchain-text-splitters==1.1.2
chromadb==1.5.9
pydantic==2.13.4
yfinance==1.4.1
pandas==3.0.3
numpy==2.4.6
ollama==0.6.2
mcp==1.29.1
requests==2.34.2
websocket-client==1.9.0

# Directly imported by dashboard/app.py and main.py's `dashboard` command
# (Phase 13). Previously left undeclared on the reasoning that both ship as
# transitive dependencies of mcp/chromadb -- true today, but not a
# reproducibility guarantee for a direct import; a repository reconciliation
# audit (pre-Phase-16) flagged this as a real gap. Pinned to the versions
# already in venv/, same convention as everything else in this file.
starlette==1.6.0
uvicorn==0.49.0

# Directly imported by market_data/universe.py (Phase 18) for
# MarketUniverse.from_yaml_file -- previously a transitive dependency of
# langchain only, now imported by name directly. Pinned to the version
# already in venv/, same convention as the rest of this file.
pyyaml==6.0.3

# test-only
pytest==9.1.1
