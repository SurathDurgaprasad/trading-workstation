# Direct dependencies actually imported by this project's code, pinned to
# the versions already installed in venv/ at the time of the Phase 1/2
# audit-and-extend pass. This file did not exist before; it documents the
# existing environment rather than introducing new pins.
#
# venv/ has many more (transitive) packages than this — this file is the
# direct-dependency contract, not a full `pip freeze`.

langgraph==1.2.4
langchain==1.3.9
# Final-product-hardening: bumped from 1.3.4 (PYSEC-2026-2192, a
# filesystem-path-confinement issue in langchain's agent file-search
# middleware and prompt/chain-config loaders -- this project imports
# only langchain_core/langchain_text_splitters/langchain_community/
# langchain_chroma, never those specific vulnerable components, so this
# was not actually exploitable here, but the patch bump is low-risk and
# closes it anyway). See SECURITY.md for the full dependency-audit
# finding, including chromadb (below), which pip-audit also flags with
# no available fix version.
langchain-core==1.6.3
# Bumped from 1.4.1 alongside the langchain 1.3.9 bump above --
# langchain==1.3.9 itself requires langchain-core>=1.4.6, so the old
# 1.4.1 pin was a genuine, unsatisfiable combination (ResolutionImpossible
# in a real `pip install -r requirements.txt` from a fresh venv --
# found by this campaign's own clean-install verification, not merely
# assumed; the existing dev venv had already been silently upgraded to
# a compatible langchain-core by `pip install langchain==1.3.9` itself,
# which is exactly why this drift wasn't caught by the test suite alone
# and needed a real, separate clean-install check to surface).
langchain-community==0.4.2
langchain-chroma==1.1.0
langchain-ollama==1.1.0
langchain-text-splitters==1.1.2
chromadb==1.5.9
# 4 known CVEs (PYSEC-2026-311/3813/3814/3815) exist for this version,
# all specifically in chromadb's networked, multi-tenant HTTP server
# mode (/api/v2/tenants/.../collections endpoints, RBAC authorization).
# This project uses chromadb exclusively as an embedded, local,
# file-backed store (langchain_chroma.Chroma(persist_directory=...) --
# see rag/retriever.py, rag/vector_store.py) -- the vulnerable HTTP
# server is never started, so these CVEs are not exploitable in this
# deployment. No fix version is listed by pip-audit; left pinned rather
# than bumped speculatively. See SECURITY.md.
pydantic==2.13.4
yfinance==1.4.1
pandas==3.0.3
numpy==2.4.6
ollama==0.6.2
mcp==1.29.1
requests==2.34.2
websocket-client==1.9.0

# Directly imported by dashboard/app.py and main.py's `dashboard` command
# (Phase 13). Previously left undeclared on the reasoning that both ship as
# transitive dependencies of mcp/chromadb -- true today, but not a
# reproducibility guarantee for a direct import; a repository reconciliation
# audit (pre-Phase-16) flagged this as a real gap. Pinned to the versions
# already in venv/, same convention as everything else in this file.
starlette==1.6.0
uvicorn==0.49.0

# Directly imported by market_data/universe.py (Phase 18) for
# MarketUniverse.from_yaml_file -- previously a transitive dependency of
# langchain only, now imported by name directly. Pinned to the version
# already in venv/, same convention as the rest of this file.
pyyaml==6.0.3

# test-only
pytest==9.1.1
hypothesis==6.168.0
# Autonomous hardening cycle 7 -- property-based testing for the highest-
# risk deterministic pure functions/state machines (risk/sizing.py,
# risk/engine.py, market/indicators.py, paper/store.py transitions):
# generated adversarial inputs (NaN, Inf, extreme magnitudes, degenerate
# series) beyond what hand-written example-based tests enumerate. Test-
# only (no production import anywhere) -- deliberately bounded profiles
# (max_examples capped), never left at Hypothesis's own unbounded
# defaults, per this project's "no fake coverage / no meaningless test
# explosion" discipline. sortedcontainers is its only transitive
# dependency, already present in venv/ (a chromadb/langchain transitive
# dep) before this addition.

# Directly imported by ml_research/ (Phase 1 quantitative prediction
# foundation, docs/research/ML_PHASE1_TRIPLE_BARRIER_BASELINE_
# PREREGISTRATION.md) -- scikit-learn's LogisticRegression is the only
# model in scope for this phase; pyarrow backs pandas' Parquet read/write
# for the feature and label datasets. sklearn was already present in
# venv/ (transitive); pyarrow was newly installed for this phase. Both
# pinned to the versions in venv/ at install time, same convention as
# the rest of this file.
scikit-learn==1.9.0
pyarrow==25.0.1

# OpenAI Intelligence Integration mission (2026-09-16): the advisory LLM
# provider used when AI_PROVIDER=openai (core/config.py, llm/provider.py).
# langchain-openai gives ChatOpenAI the same with_structured_output()
# interface agents/analyst.py already uses for ChatOllama, so no callers
# needed to change. Newly installed for this phase; not present before.
langchain-openai==1.6.2
openai==3.14.1
